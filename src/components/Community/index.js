import React from "react";
import Button from "../Button";
import { Container, Title } from "./CommunityElements";

const Community = () => {
  return (
    <Container>
      <Title>ready to build your community?</Title>
      <Button />
    </Container>
  );
};

export default Community;
